export interface selectItem {
  value: string;
  label: string;
}

export interface userLoginInfo {
  gitEmail: string;
  profileImage: number;
  name: string;
  totalCredit: number;
  isInvited: boolean;
}
